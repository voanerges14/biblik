create table library (
  id INT NOT NULL AUTO_INCREMENT PRIMARY KEY,
  name varchar(50),
  author varchar(50),
  type VARCHAR(50),
  nPages int
);

//Delete table
DROP TABLE libraty

//clean table
DELETE FROM library

